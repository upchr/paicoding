# 问题：当前项目怎么处理多环境配置的？

## 后端多环境配置

### 1. Maven Profiles 配置
- 定义了 4 个环境：`dev`（开发）、`test`（测试）、`pre`（预发）、`prod`（生产）
- 默认使用 `dev` 环境
- 配置位置：`pom.xml` 的 `<profiles>` 节点

### 2. 环境配置文件结构
```
paicoding-web/src/main/resources-env/
├── dev/      # 开发环境
├── test/     # 测试环境
├── pre/      # 预发环境
└── prod/     # 生产环境
```

每个环境目录包含：
- `application-dal.yml` - 数据库、Redis、ElasticSearch 配置
- `application-web.yml` - Web 配置（日志路径、模板缓存等）
- `application-image.yml` - 图片处理配置
- `application-rabbitmq.yml` - 消息队列配置
- `application-ai.yml` - AI 相关配置

### 3. 环境切换方式
```bash
# 开发环境（默认）
mvn clean install -DskipTests=true

# 测试环境
mvn clean install -DskipTests=true -Ptest

# 预发环境
mvn clean install -DskipTests=true -Ppre

# 生产环境
mvn clean install -DskipTests=true -Pprod
```

### 4. 关键差异配置
- **数据库密码**：dev 环境有密码，prod 环境无密码
- **Redis 密码**：dev 环境有密码，prod 环境无密码
- **日志路径**：dev 使用 `logs`，prod 使用 `/home/ubuntu/tech-pai/logs`
- **模板缓存**：dev 关闭缓存，prod 开启缓存
- **SQL 日志**：dev 开启 SQL 日志输出

## 前端多环境配置

### 当前状态
前端未配置多环境，后端 API 地址硬编码在 `pai-coding-front/src/http/URL.ts` 中：

```typescript
export const BASE_URL = "http://localhost:8081"
export const WS_URL = "ws://localhost:8081"
```

### 建议改进
如需前端多环境配置，可以使用 Vite 的环境变量功能（`.env.development`、`.env.production` 等）。